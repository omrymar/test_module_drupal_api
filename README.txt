// After the module installation:
1. Install the devel module to generate the article nodes(optional if the article nodes are not exist);
2. Go to the /admin/structure/block and place blocks:
"Node views statistics" and "Random quotes" to the content region.
3. Save the changes in the blocks.

